const path = require('path');
const {expect} = require('chai');
const {calculateDivisors} = require(path.join(__dirname, '..', './calculateDivisors.js'));

describe('calculateDivisors()', function () {
  it('ADD FIRST TEST AND RENAME', function () {
    
  });
});